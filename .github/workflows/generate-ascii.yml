name: Generate ASCII Artwork


on: push

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v5

        - name: list repo files
          run: ls -ltra

        - name: Exexuting shell script
          run: ./ascii-script.sh

        # - name: list files
        #   run: ls
          
        # - name: Read file
        #   run: cat README.md