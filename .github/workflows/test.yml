name: My First workflow


on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v5
        - name: list and read files
          run: |
            echo "My first Github Actions Job"
            ls -ltra
            cat README.md

        - name: generate ASCII artwork
          run: cowsay -f draon "Run for cover,I am a Dragon..... RAWR" >> dragon.txt

        # - name: list files
        #   run: ls
          
        # - name: Read file
        #   run: cat README.md